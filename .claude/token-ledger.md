# Agent token ledger

Running record of engine-coder task runs and their reported token usage,
maintained by the supervising session after every run. Purpose: budget
awareness (account usage limits are NOT queryable programmatically — this
ledger is the only consumption signal we have) and scoping future tasks.

**Budget policy:** target <= ~150k tokens per agent run. Larger features get
split into sequential runs. Every run follows the wip-branch checkpoint
protocol (see `.claude/agents/engine-coder.md`) so a usage-limit or session
death never loses more than one milestone.

| Date | Task | Tokens | Outcome |
|---|---|---|---|
| 2026-07-09 | Ctrl+D carries entity state | 70,765 | completed, merged |
| 2026-07-10 | Depth resolve + Unreal fly controls | 125,164 | completed, merged |
| 2026-07-10 | Editor platform (menus/docking/settings) | 249,857 | completed, merged (over budget — should have been 2 runs) |
| 2026-07-10 | OpenGL GPU backend (moderngl) | 300,869 | completed, merged (over budget — should have been 2 runs) |
| 2026-07-10 | DX12/Vulkan dispatch attempt | 47,763 | wasted: agent re-delegated instead of implementing (rule added) |
| 2026-07-10 | DX12/Vulkan backend (wgpu) | unknown (nested + resumed runs) | DIED AT SESSION USAGE LIMIT mid-verification; work salvaged to wip/dx12-vulkan-backend at 1cc35d6; resumed |
| 2026-07-10 | DX12/Vulkan resume (transcript resume) | unknown | DIED AT LIMIT AGAIN mid-settings-fix; salvaged at 57bf45f. Lesson: resuming a huge transcript re-reads it all at cold rates — after a death, start a FRESH agent from the wip branch + HANDOFF instead. Remaining: settings row-spacing fix + full verification battery. |
| 2026-07-10 | DX12/Vulkan finisher (fresh from checkpoint) | 77,335 | completed under budget; judged + merged to main |
| 2026-07-11 | Environment assets (sun/sky material/GI/fog volumes) | 461,730 cumulative (5 resumes incl. 2 limit deaths) | completed; 1 review finding (GI receiver gating) fixed on send-back; judged + squash-merged e7b7735. Over budget: should have been 2-3 runs |
| 2026-07-11 | Window management (minimize/close/registry/reset) | 143,784 | completed under budget; judged + squash-merged |
| 2026-07-12 | wgpu visual parity (sun disc/GI/fog volumes) | 170,917 (+ ~91k wasted: agent re-delegated on first call despite the rule — caught, sent back; its stray nested agent burned 46k more) | completed; judged + squash-merged 52f0601. Slightly over budget. Lesson: restate the anti-delegation rule IN the task prompt, not just the agent definition |
| 2026-07-12 | Panel resizing + fullscreen adaptive layout | 168,479 | completed; judged + squash-merged c73f4bd. Slightly over budget. Restating the anti-delegation rule in the prompt worked — clean single run. Agent found + fixed a real fullscreen-size bug (display.Info vs desktop size). Note: agent accidentally overwrote the user's per-user settings.json (untracked, regenerates) — remind future briefs to set it aside FIRST |
| 2026-07-12 | Content browser folder tree | 220,697 cumulative (incl. review send-back) | completed; 1 review finding (root/no-rename None collision) fixed on send-back; judged + squash-merged 979b8b8. Over budget: ~40k went to chasing an FPS anomaly that turned out environmental (this working dir benches ~35% slow vs a fresh worktree — benchmark from clean worktrees from now on) |
| 2026-07-12 | Viewport toolbar + editable transform fields | 174,787 | completed clean; judged + squash-merged f1b556d. Slightly over budget. FPS worktree-vs-in-place gap flipped direction vs previous run — conclusion: benchmark IN PLACE with settings.json aside (see HANDOFF note); the worktree advice from the previous run is withdrawn |
| 2026-07-12 | Settings isolation + dock drop-zones (bugfix) | 124,246 | completed under budget; judged + squash-merged 5b3f6eb. Root cause of the user's "broken Details panel": test suites were writing mid-test layout state to the real settings.json (supervisor diagnosed; also repaired the user's settings.json directly). Tests now use PYENGINE_SETTINGS temp paths + a no-pollution guard |
| 2026-07-12 | FBX export from browser (slate run 1/3) | 119,909 | completed clean under budget; judged + squash-merged 63d0e63. Blender import needs one manual user check (unverifiable here) |
| 2026-07-12 | Texture assets + UVs + TexCoord/TextureSample (slate run 2/3) | 205,088 | completed single-pass; judged + squash-merged bbc5f02. Over budget (built the whole UV foundation). Agent skipped gl/wgpu in its battery — supervisor ran them (passed) |
| 2026-07-12 | UE node overhaul + material drag-drop (slate run 3/3) | 176,682 cumulative (incl. send-back) | completed; 1 review finding (Output pin rename broke texture_checks; connect() silently accepted dead pins) fixed on send-back; judged + squash-merged fcd290c. Agent's battery skipped the 3 suites most coupled to its changes — supervisor caught the failure. Reinforce: the brief must name every suite AND the agent must run all of them |
| 2026-07-12 | PBR foundation + CPU renderer (PBR 1/3) | 190,326 | completed clean; judged + squash-merged b97f515. Over budget (golden-fixture infra + FPS fast-path work). Good spec_scale compat-gate design |
| 2026-07-12 | PBR OpenGL parity (PBR 2/3) | 135,531 | completed clean under budget; judged + squash-merged 53db243. Naming all ten suites in the brief worked — full battery run |
| 2026-07-12 | PBR wgpu parity (PBR 3/3) | 140,209 | completed clean under budget; judged + squash-merged c314422. 3-way parity diffs exactly 0.0; also fixed latent missing-PI WGSL bug and flagged stale line in engine-coder.md (supervisor corrected it) |
| 2026-07-13 | DX12 default renderer (CPU opt-in) | 82,477 | completed clean under budget; judged + squash-merged 2df73cd. User preference saved to supervisor memory |
| 2026-07-13 | Material editor UX (UE menus + preview panel) | 164,685 cumulative (incl. send-back) | completed; 1 cosmetic finding (overlapping menu text) fixed on send-back; judged + squash-merged 79dc8a9 |
| 2026-07-13 | Material types + transparency (2 agents: checkpoint stop at M2 + fresh finisher) | 148,601 + 194,948 | completed; judged + squash-merged a284f06. Checkpoint protocol worked exactly as designed � clean stop, fresh resume from HANDOFF, zero waste |
| 2026-07-13 | Fix text-input duplication (fixed-timestep consume-once) | 73,659 | completed clean under budget; judged + squash-merged. Supervisor pre-diagnosed root cause in the brief � cheapest run yet with a real engine bug |
| 2026-07-13 | Fix ctx-menu click crash (event-path regression tests added) | 113,618 | completed clean under budget; judged + squash-merged. Supervisor reproduced with traceback before briefing. Lesson encoded in tests: UI suites must drive the real event path, not handlers directly |
| 2026-07-14 | Snapping + Alt-drag duplicate (2 agents: M1 checkpoint + M2-4 finisher) | 101,712 + 296,065 | completed; judged + squash-merged abf4a00. Finisher WAY over budget (296k) — should have been split again at M2/M3. Supervisor independently verified floor snap + Alt-drag through real code path (agent tests fudge held-keys via get_pressed patch, legitimately). All 13 suites pass |
| 2026-07-14 | Multi-selection foundation (run 2a of pivot slate) | 286,562 | completed; judged + squash-merged d5eefef. Over budget again (286k) — the self.selected->property refactor + batch-op plumbing was bigger than scoped. Supervisor independently verified translate-moves-all + batch dup/delete through real path. All 14 suites pass. NOTE: this session's finishers keep running 2x budget; scope run 2b tighter |
| 2026-07-14 | Blender pivot modes (run 2b, final of slate) | 207,406 | completed clean single-pass; judged + squash-merged 9feace2. Over budget (207k). Supervisor independently verified all 4 pivot-mode rotate outcomes + single-select reduction via exact-90deg drive through real code path. All 15 suites pass |
| 2026-07-14 | Box/marquee selection | 192,036 | completed clean single-pass; judged + squash-merged. Over budget (192k). Supervisor independently verified marquee selects exactly enclosed entities + Shift-add via real event path. All 16 suites pass |
| 2026-07-14 | 3D-cursor pivot (fifth mode) | 241,874 (+ prior attempt died at session limit, 0 output) | completed; judged + squash-merged. Over budget (242k). Supervisor independently verified the single-selection-orbits-cursor differentiator + C/Shift+C keybinding split. All 17 suites pass. First attempt hit a session usage limit having produced nothing — checkpoint-early instruction added to the resume brief worked |
| 2026-07-14 | Docking previews + tabbed panels | 242,789 | completed single-pass; judged + squash-merged. Over budget (243k) AND violated the mandatory M1 checkpoint (single commit for all 4 milestones — flagged). Supervisor verified tab join/switch + ghost preview visually via real-event drag, and disproved an apparent 45% demo-dx12 regression as environmental variance (61-102 FPS on identical code) via same-environment A/B. All 18 suites pass |
| 2026-07-14 | Unreal-style import-options dialog | 226,988 | completed; judged + squash-merged. Over budget (227k). Supervisor DIAGNOSED the user's "FBX won't drag" report first: not a drag bug — asset was 0.12u (cm scale, invisible) + filed in a hidden folder. Dialog fixes both (scale bake + browser nav). Independently verified user scenario end-to-end. All 19 suites pass |
| 2026-07-14 | Distance-based LOD (import/LOD slate run 2) | 359,005 | completed; judged + squash-merged. WORST overrun of the session (359k) — a genuinely large cross-cutting change (decimation + all 3 renderers + shadow/GI LOD0-pinning) that should have been 2-3 runs. BUT checkpointed all 4 milestones individually (good). Supervisor independently verified byte-identical low-poly A/B, LOD selection+hysteresis, LOD0-pinned shadows, and the speedup. All 20 suites pass. LESSON: features touching all 3 render backends must be pre-split |
| 2026-07-14 | Blueprint asset + script editor + compile (run 1 of 2) | 285,359 | completed; judged + squash-merged. Over budget (285k) though all 4 milestones were checkpointed individually. Built the engine's first multi-line text editor. Agent also found+fixed a latent per-frame AttributeError (has_mesh on BlueprintAsset). Supervisor independently hammered compile_blueprint with 11 adversarial scripts — never escapes, line numbers accurate — and screenshot-verified the error highlight. All 21 suites pass. CAVEAT surfaced to user: no infinite-loop guard on exec |
| 2026-07-14 | High-poly shadow/GI freeze fix | 256,921 + 319,540 (agent, over 2 stops) + supervisor finish | completed; judged + squash-merged. Agent BURNED ~575k over two stops and its fix was INEFFECTIVE for the actual user asset (only handled meshes WITH precomputed LODs; the user's Gat imported pre-LOD has none -> still 283s freeze). Supervisor diagnosed the freeze (295s hard number), caught the gap, and FINISHED THE FIX DIRECTLY (on-demand cached decimation proxy above SHADOW_PROXY_THRESHOLD) rather than send back a 3rd time: 295s->8.9s, byte-identical low-poly, all 21 suites + a new regression test. LESSON: for a perf fix, the brief MUST name the real asset's exact conditions (no precomputed LODs); and when an agent thrashes, supervisor implements small well-diagnosed fixes directly |
| 2026-07-14 | Collapsible side toolbar + engine console (console run 1 of 2) | 328,983 | completed; judged + squash-merged. Over budget (329k) but all 4 milestones checkpointed. Agent fixed a real forward-compat bug (saved layout discarded on new panel id). Supervisor verified message bus (import/compile/bake) + bake logs via the REAL run loop (agent left NO bake-log test — gap noted) + screenshot. All 22 suites pass |
| 2026-07-14 | Uncapped FPS + optional clamp + smoothed readout | 217,169 | completed clean; judged + squash-merged. Over budget (217k) though 2 clean milestones. Deliberately SPLIT the async bake out (too risky to bundle) — good discipline call. Supervisor independently verified uncap default, cap throttle (31 vs 1104 fps A/B), EMA smoothing, migration. All 23 suites pass |
| 2026-07-14 | Async non-blocking lighting bake (RT responsiveness 1/2) | 404,685 | completed; judged + squash-merged. WORST OVERRUN OF THE PROJECT (405k, ~2.7x) — a threading rewrite of the bake. BUT correct: safe snapshot->pure-bake refactor first (byte-identical), then worker thread with atomic install + coalescing; renderers needed zero changes (pre-warms existing caches). Supervisor independently verified: sync path 0.0/0.0 vs main, async CONVERGES to sync 0.0/0.0, dispatch 1.3ms non-blocking, all 24 suites. User chose phased (async now, BVH next). LESSON: a threading rewrite is inherently multi-run — should have hard-split M1(refactor)/M2(threading) into separate agent runs |
| 2026-07-14 | BVH ray-trace acceleration (RT responsiveness 2/2) | 188,745 | completed; judged + squash-merged. Over budget (189k) but BEST of the recent big runs + checkpointed 3 milestones. Isolation-first strategy worked: BVH tested vs brute-force standalone before integration. Supervisor independently verified any_hit EXACT (0 mismatches), nearest exact (t-diff 0.0), starter scene byte-identical (BVH path, 0 px), real Gat bake 295s->1.14s. All 25 suites pass. Gated behind BVH_THRESHOLD=256 so low-poly stays brute-force-identical. RT effort COMPLETE (proxy + async + BVH: 295s freeze -> 1.14s non-blocking) |
| 2026-07-25 | Blueprint posed meshes (run 2a) — attempt 1 | 0 (died before any output) | DIED AT SESSION USAGE LIMIT having produced NOTHING: no wip branch, no commits, tree untouched. Supervisor confirmed nothing to salvage. Second zero-output limit death of the project; both cost a whole usage window. Relaunched fresh with a mandatory STEP 0 (push branch + HANDOFF skeleton BEFORE any code) |
| 2026-07-25 | Blueprint posed meshes (run 2a) — attempt 2 | 260,487 | completed M1-M3, judged + squash-merged d33ca4a. Over budget (260k, 1.7x) despite being a deliberate half-split. STEP 0 WORKED — skeleton + 3 milestones were all pushed, so the agent stopping mid-M4 cost nothing. Agent stopped while waiting on a background battery it never confirmed, leaving the new suite UNTRACKED; supervisor checkpointed it and ran M4 itself. Supervisor independently verified the merge math (byte-identical gate across all 8 arrays, TRS posing at 0.000e+00 error, negative-scale winding flip proven load-bearing via an inward-facing control, heterogeneous per-face array alignment, save/load round-trip) and disproved an apparent FPS regression via same-environment A/B vs a main worktree. All 26 suites pass. LESSON: "run the battery" is not a milestone an agent can be trusted to close out — it stops at the wait; budget for the supervisor running the final battery |
| 2026-07-25 | Blueprint runtime + loop guard (run 2b, final) | 230,168 | completed M1-M3, judged + squash-merged 2653b35. Over budget (230k, 1.5x). Agent got the two hard constraints right unprompted: honest timeout docs (no ctypes fake-kill) and a proxy that isolates ONLY blueprint behaviors, leaving Scene.update's built-in loop unwrapped. STOPPED MID-M4 WAITING ON A BACKGROUND BATTERY — the identical failure to 2a, despite the brief explicitly forbidding it; supervisor checkpointed the untracked suite and ran M4. Supervisor review found TWO defects its green tests missed: (1) runaway exec threads accumulated one per Compile press — measured 8.83 FPS clean -> 0.66 with one alive (13x) -> 0.31 with two (29x); fixed with MAX_RUNAWAY_THREADS=2 + a blocked verdict (cap 2 not 1: the cap gates the NEXT exec and a cap of 1 locks the author out of compiling their own corrected script — found because cap=1 broke a valid-script test). (2) the shadow warning watched position only, but ShadowTracer keys on the full transform matrix, so rotate/scale-only animation thrashed the bake silently. Supervisor also supplied the shadow-cache measurement the brief demanded and the agent skipped (2.44 FPS moving+casting vs 6.30 mitigated vs 7.74 static), which VALIDATED the agent's no-auto-flip decision. All 27 suites pass. LESSON: "don't stop at the battery" in the brief did NOT work — assume the supervisor runs the final battery and budget for it |
| 2026-07-25 | Per-corner UV foundation (per-pixel texturing 1/4) | 242,206 | completed all 4 milestones single-pass; judged + squash-merged 0f97e99. Over budget (242k) but a genuinely foundational run touching mesh/FBX/npz/LOD. FIRST agent this session to run the full battery in the FOREGROUND and finish verification before stopping (the brief said to; the two before it ignored the same instruction) — nothing was left uncommitted. Agent DEVIATED from the brief with good reason: told to derive face_uvs from corner_uvs for one source of truth, it found float division does not commute with averaging (~1 ULP drift on cylinder/cone/icosphere/torus/checkerboard) which would have broken the byte-identical gate, so it kept two projections + a committed 1e-9 agreement assertion. Supervisor independently re-proved the byte-identical gate vs a main worktree (42 arrays, array_equal, non-vacuous) and — the check that mattered — measured that corner UVs actually VARY (100% of faces): a conforming implementation copying one UV to all four corners would pass every shape/padding assertion while delivering nothing. Real prize: fbx.py was computing per-polygon-vertex UVs and discarding them; that detail now survives, proven by a hand-built binary FBX since export_fbx still flattens. All 27 suites pass; A/B vs main shows no regression. NOTE: the agents own A/B worktree blocked git checkout main at merge time — remove worktrees before merging |
| 2026-07-26 | CPU per-pixel texture sampling (per-pixel texturing 2/4) | 302,721 | completed all milestones single-pass; judged + squash-merged. WORST of this slate (302k, 2x) but the riskiest run in it, and it landed with a zero-pixel gate. Ran the battery in the FOREGROUND and finished verification before stopping (2nd run in a row to comply). Design: only a texture feeding an Output channel on a bare wire is sampled per pixel; wrapped/procedural cases fall back to the per-face bake. Agent independently reached the key testing insight — a box-projected quad is globally affine, so an always-use-triangle-1 barycentric bug would COINCIDENTALLY PASS an ordinary test — and built a non-affine twisted quad to catch it. Supervisor verification: (1) checked the byte-identical golden fixture was NOT CIRCULAR by re-rendering the starter scene from a real main worktree at 76cb82b — fixture vs real main 0 px, main vs branch 0 px, 948 distinct colours so non-vacuous; a fixture captured from the branch would have proven nothing. (2) re-derived the quad case by hand on a twisted quad: correct v=0.46667 vs buggy 0.66667, discriminates by 0.20; diagonal continuous, degenerate padded half no NaN. (3) visual A/B render: 64 distinct colours across one face via direct binding vs a flat fill via the multiply-wrapped fallback. All 27 suites pass. Perf: untextured unchanged (28.43 vs 28.45ms, and the 0-px gate proves the path untouched); realistic textured ~1%; honest ceiling ~2x per-call when a textured quad fills the frame. Live loose ends recorded in CLAUDE.md: LOD swim (most user-visible), GI stays per-face, opacity bindings structurally unreachable for translucents, nearest-neighbour sampling |
| 2026-07-26 | OpenGL per-pixel texture sampling (per-pixel texturing 3/4) | 240,539 | completed all milestones single-pass; judged + squash-merged. Over budget (240k vs the 135k the equivalent PBR-slate GL run took). Battery run in the FOREGROUND, verification finished before stopping (3rd run in a row to comply). Handled the shared-file coupling trap correctly: gpu_geometry._build_uv is simply NOT imported by wgpu_renderer.py, which has an EMPTY diff, so run 4 starts from a clean backend. Agent found TWO silent-corruption traps by hand-tracing rather than by luck: (1) V-FLIP — CPU sample_texture maps v=0 to the image BOTTOM row, opposite GL convention, fixed at upload; (2) render_scale — a GPU-vs-CPU comparison at the default scale 3 gives 1587/30000 differing px vs 173/30000 at scale 1, which a weaker run would have absorbed by widening the tolerance. Supervisor verification: (1) re-rendered gl_checks scene from a real main worktree at 01a55c8 through the standalone GL context — golden vs real main 0 px, main vs branch 0 px, 582 distinct colours, so the fixture is NOT circular (same check as run 2, now standard); (2) TESTED THE TOLERANCE ITSELF by measuring the failure mode it exists to catch — a silent flat-per-face fallback gives mean 13.3 / 25.2% differing against a mean<3.0 / <3% gate and 0.266 / 0.58% actual, i.e. 4.4x and 8.4x of headroom, so the gate discriminates. All 27 suites pass. LESSON worth keeping: verifying a tolerance means measuring what a real bug would produce, not eyeballing whether the number looks tight |
| 2026-07-26 | wgpu per-pixel texture sampling (per-pixel texturing 4/4, FINAL) | 271,954 | completed all milestones single-pass; judged + squash-merged. Over budget (272k vs the 140k the equivalent PBR-slate wgpu run took). Battery in the FOREGROUND, verification finished before stopping (4th run in a row). Cleanly isolated: renderer.py, gl_renderer.py and gpu_geometry.py all byte-untouched. BEST judgment call of the slate: told not to assume GLs V-flip answer transferred, it did not merely re-derive the same fix — it took a DIFFERENT route, using textureLoad instead of textureSample (rgba32float is unfilterable-float and cannot legally back textureSample without a non-portable device feature), hand-rolling the index to reproduce sample_textures own formula so no flip is needed at all, and matching the files existing shadow/gi/ies exact-texel pattern rather than adding a second one. Parity mean 0.266 / 173 of 30000 px, identical to GLs numbers — which is what correctness predicts for two hardware rasterisers against one CPU reference. Did the discrimination check unprompted-in-brief at 13.220 / 25.23%, agreeing with the supervisors own independent 13.341 / 25.23%. Supervisor verified the third golden non-circular vs a real main render (0 px, 582 colours) — all three per-pixel goldens now confirmed. All 27 suites pass; DX12 is the users DEFAULT renderer so wgpu_checks was the highest-stakes suite. Agent also surfaced a latent bug outside scope (both GPU backends key geo caches on id(mesh) with no content check; CPython address recycling can cause false cache hits) — filed as a separate task, not fixed here. SLATE COMPLETE across CPU/GL/wgpu: 4 runs, 1057k tokens total, every run over the 150k target but each landed with a zero-pixel gate rather than a judgment call |
| 2026-07-26 | Fix id()-keyed GPU cache false hits (both backends) | 176,585 | completed clean single-pass, no send-back; judged + squash-merged. CHEAPEST run of the session and the only recent one near budget — because the supervisor REPRODUCED the bug with a traceback before briefing (gl_renderer.py:927 / wgpu_renderer.py:1101 -> gpu_geometry.py:39 IndexError) and shipped the repro script with the brief. Reproducing also changed the DESIGN: it showed the real defect is SILENT CORRUPTION (a cache hit rebuilds per-face attrs against the stale entry face_id_tri; IndexError only when face counts differ, silently wrong geometry when they coincide), which ruled out the count-sidecar option the task description had suggested and forced the monotonic-serial fix. Agent chose itertools.count() serials on Mesh/Entity, noting the __next__ atomicity matters for meshes built off the main thread by blueprint behaviours. Supervisor verified: (1) own pre-brief repro now runs 60/60 clean on both backends with address recycling still logged, so the hazard is exercised not avoided; (2) the new regression tests re-run in a worktree of pre-fix c46d706 both FAIL (exit 1) with an exact signature — and they target the SILENT case using same-topology different-size cubes, so pre-fix there is no exception at all; (3) all three per-pixel goldens still 0 px, the fix moved nothing. All 27 suites pass; cache-hit A/B shows no regression. Agent also found MORE id()-as-identity uses (color_id/pbr_id/opacity_id version stamps with an INVERTED failure mode, and _get_env_tex id(env.image)) and correctly SCOPED THEM OUT rather than widening the change — supervisor confirmed they are real and filed them separately. LESSON, now in HANDOFF: reproducing before briefing is the highest-leverage supervisor habit; it buys a cheaper run AND a better design |
| 2026-07-26 | wgpu directional (sun) shadow attenuation | 174,679 + 208,650 (send-back, mostly resumed transcript: 25 tool calls vs 112) | completed; 1 REVIEW SEND-BACK; judged + squash-merged. Implementation landed correct first time and surgical (wgpu_renderer.py only, 68 lines; gl_renderer/renderer/raytrace byte-untouched), mirroring GLs dlShadowTex pipeline field-for-field and — the trap named in the brief — multiplying lambert AT ITS POINT OF COMPUTATION so specular is attenuated too, not just the two diffuse branches. SEND-BACK REASON: shipped with NO committed test. All verification lived in throwaway scripts; wgpu_checks sun-DISC check passes no tracer so it never touches dl_shadow_tex, and all four goldens are no-sun scenes — the new path had zero coverage on the users DEFAULT renderer. Sent back for a committed check rendering a Sun WITH a tracer, asserting EXACT GL/dx12 parity (correctly NOT toleranced: both index the same per-face tracer output, no per-pixel resampling, so no float-divergence source), plus a non-vacuity assertion that shadow_depth=0 kills the shadow. Supervisor verified: dx12 output genuinely changes main->branch and GL-vs-dx12 is byte-identical (0.000, 0 px) after; and re-ran the new test against pre-fix main in a worktree — FAILS with max 50.0 / mean 1.6282, matching the original divergence. All 27 suites pass. SUPERVISOR MISTAKE WORTH RECORDING: my own verification scene put the shadow out of frame and understated the pre-fix gap as 0.06% vs the real 4.16%; looking at the RENDER settled it in seconds where the numbers looked like a contradiction. Build sun scenes by instantiating the real Sun asset + scene.update (SunController drives light direction) and floor.casts_shadow=False. LAST ITEM ON THE USERS PRIORITY LIST — wgpu now at full visual parity with GL |
